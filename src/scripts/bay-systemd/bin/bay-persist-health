#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=lib.sh
source "${SCRIPT_DIR}/lib.sh"

if ! http_check "$(persist_url)"; then
  bay_log "persist is unhealthy"
  exit 1
fi

bay_log "persist healthy"
