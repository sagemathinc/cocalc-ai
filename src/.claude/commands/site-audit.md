---
description: Refresh the finite ranked public-site punch-list against the Brief, via ONE delegated subagent.
---

Refresh the finite ranked punch-list without polluting this context.

Delegate to ONE subagent (Explore or general-purpose) — keep screenshots and dozens of
source files out of the driver's context. Instruct it to audit the public-site surfaces
named in `docs/landing-page-issues-and-plans.md` against `docs/landing-page-brief.md` and
return ONLY a ranked, finite punch-list: for each surface — the buyer + question it serves,
its one next step, the gap to the home benchmark, what to **remove**, and a severity/effort.

Then YOU reconcile its list into `docs/landing-page-issues-and-plans.md`: update statuses,
add genuinely new items, but **do not reopen items already marked done** (the D2
done-condition). Do not edit any page in this command — audit only.
