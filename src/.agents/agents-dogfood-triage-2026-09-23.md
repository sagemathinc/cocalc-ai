# Agents dogfood triage (2026-09-23)

## Purpose and baseline

This is a decision queue, not an instruction to implement every note. The source is the maintainer's two-day `agents.tasks` dogfood log (35 active entries, 2 deleted entries). A note is evidence of an experience, not yet a verified root cause or an approved design. Do not copy its private screenshots, conversation URLs, or agent-message contents into a public PR.

This follow-up is stacked on draft [#668](https://github.com/sagemathinc/cocalc-ai/pull/668), head `8e28b41b5e` at triage time. Keep #668 unchanged. Recheck each report against the actual deployed build and this branch before changing code; some observations may be stale, environment-specific, or affected by work already in #668. In particular, #668 includes composer cursor/draft changes and a Library/workbench redesign, but neither proves the remaining reports fixed. No active note is marked resolved without a reproduction or acceptance check.

Use `P0` for a candidate release blocker if reproduced, `P1` for high-friction or trust failures, `P2` for bounded polish, `Decision` for behavior that needs product agreement, and `Hold` for insufficient evidence or a broader design discussion. Priorities are provisional.

## First pass: verify before release

| Note                   | Priority              | Triage and next acceptance check                                                                                                                                                                                                                                                                                                                                 |
| ---------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `9cb0a713`             | P0                    | Human-only chat shows "To Agent". Reproduce in a thread with no agent binding, then trace `ComposerDeliverySelector` visibility in `frontend/chat/composer.tsx`. Human chat must offer only valid human delivery; cover new/existing threads and switch between them. **First issue to work.**                                                                   |
| `9e3806c9`             | P0                    | Same thread open in Agents and legacy `.chat` allegedly duplicates composer text. Reproduce with both views mounted, typing and sending once. Establish whether shared draft state, editor event replay, or two writers caused it. Do not simply prohibit two views until the data-loss mechanism is known; require a no-duplicate/no-lost-text regression test. |
| `f84bae48`             | P0                    | A peer message appeared received but did not wake its agent. Correlate send admission, inbox persistence, execution principal, wake scheduling, and recipient turn state for one attempt. A visible "received" state must not imply execution. Add a durable end-to-end test for wake or an explicit non-wake outcome.                                           |
| `9947b2cb`             | P0                    | Agent credential failure when sending at turn end. Check credential lifetime and authorization/retry at admission versus delivery; distinguish expiry from revocation. Never hide or automatically duplicate an unknown-outcome send. Test an expiring credential and a queued send.                                                                             |
| `da394fbf`             | P0                    | Subscription loading can temporarily select the site-funded fallback model. Reproduce cold load and delayed entitlement lookup; an unresolved preferred model must not silently commit or send with a fallback. Test selection stability until account/project funding is known.                                                                                 |
| `8cf40e27`, `b9db4c3b` | P1, one investigation | Green running dots remain on apparently idle threads and completion/unseen status is unclear. Trace the source and freshness of `agent-running-indicator.tsx`'s queued/sent/running states, then separate "work active" from "result unseen". Avoid inventing a new database state until lifecycle evidence shows it is needed.                                  |
| `9043daa4`             | P1                    | Agents view hides the chat sync/loading state and allows an unusable composer. Show explicit initialization/readiness and disable send until the authoritative thread is writable; test delayed sync and reconnect.                                                                                                                                              |
| `530dd88c`, `153d2b6c` | P1, one investigation | Agent Network messages are hard to read, sometimes only in raw JSON/tooltips or scrolled away. First verify the exact payload and scroll behavior; then provide a readable, persistent message surface with delivery state. Do not show secret fields by default.                                                                                                |
| `04ba0bc9`             | P1                    | Current model is too subtle, especially if it changes due to funding. Make the effective model and any fallback prominent at send time. User-themed model badges are a separate design decision, not part of this fix.                                                                                                                                           |
| `c0aabc3a`             | P1, policy check      | Confirm intended agent creation and visibility behavior for ineligible projects. A UI observation does not establish an authorization flaw; if one is suspected, move investigation and details to the private `SECURITY.md` workflow before public discussion.                                                                                                  |
| `3fb335e1`             | P1                    | Agent notifications open the legacy `.chat` instead of the agent route and omit `@name`. Verify notification payload has a stable agent identity, then route to `/agents/...` where authorized; retain a safe fallback for deleted/unavailable agents.                                                                                                           |
| `66469220`             | P1                    | Duplicate Agent Network names are confusing. Check whether names are account-unique or merely labels. Design explicit choose-existing versus create-new behavior; do not silently join a same-named network without checking authority and membership.                                                                                                           |

## Bounded follow-ups

| Note       | Priority     | Triage and smallest useful slice                                                                                                                                                                                                      |
| ---------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `9297c70f` | P2           | Refreshing an agent route flashes the new-chat view. Keep the route in a loading/skeleton state until agent resolution finishes; test direct URL load.                                                                                |
| `84d71994` | P2           | New-user onboarding does not mention Agents. Review actual first-run paths and propose one entrypoint update after the core flow is stable.                                                                                           |
| `81cf5c2c` | P2           | PDF/document workbench header repeats file name and wastes height. Compare all artifact types before simplifying shared chrome.                                                                                                       |
| `c06e0b06` | P2           | Sending while dictating should stop capture and send the finalized transcript once. Define the pending-transcription state and test no partial or double send.                                                                        |
| `a901935c` | P2           | An agent waited through many turns for a peer reply although a reply can wake it. Update messaging CLI/skill guidance to distinguish admission from completion and explain when to end a turn. Verify the actual wake contract first. |
| `8ca480c3` | P2           | Heavy outer composer focus border. Test keyboard-focus visibility before removing or reducing it; preserve an accessible focus indicator.                                                                                             |
| `ca957f43` | P2           | Browser tabs all read "Agents". Put the selected agent name first, with a useful fallback for Library/new agent.                                                                                                                      |
| `0a00c045` | P2, adjacent | Add Restart prominence to the project drawer only after confirming its existing project state/permission semantics. Not an Agents-specific blocker.                                                                                   |
| `a6595083` | P2           | Artifact and Git review font size is too small and not persistent. Identify whether the existing editor font preference can be reused across workbench frames; test close/reopen.                                                     |
| `f7621de5` | P2           | Artifact opening restores irrelevant file-menu actions. Audit each frame mode before hiding commands; don't remove valid Save/TimeTravel actions globally.                                                                            |
| `56335e15` | P2           | Agent drag order differs between browsers. Inspect current storage and conflict semantics; durable account-home ordering can be a later slice without real-time sync.                                                                 |
| `b7ec734d` | P2           | Add speech-to-text for the first message in New Agent if the shared dictation control is reusable and permissions are clear.                                                                                                          |

## Decisions, not automatic fixes

| Note                   | Status             | Question to settle before implementation                                                                                                                                                                                       |
| ---------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `9c1b2ed6`             | Decision           | Should a posted message later be sendable to the agent? Reposting at the bottom preserves chronology better than moving the original; decide identity, attribution, quoting, and what "Post" promises.                         |
| `5e155d01`             | Decision           | Live-only Agent Network delivery would remove valid queued/offline use cases. First measure failures and clarify the current live/queued semantics; decide whether to change the default rather than deleting queued delivery. |
| `b09064ac`, `96f9c2bf` | Decision, one flow | What should New Agent do with zero eligible projects? Decide whether to offer Create Project, direct navigation, or an integrated flow, including project ownership and AI-enabled eligibility.                                |
| `49519048`             | Decision           | Allow agent creation with no initial message? Clarify when the agent process starts, what empty history looks like, and whether "hi" is needed at all.                                                                         |
| `2eaec91c`             | Decision           | Clicking `@name` to rename may cause accidental edits. Prefer an explicit menu/action unless user testing demonstrates a need for the shortcut.                                                                                |
| `60b5a509`             | Decision           | General sidebar/avatars/drag-handle simplification needs a concrete screenshot and target layout. Do not remove keyboard/touch reorder affordances on a vague cleanliness request.                                             |

## Hold or separate design track

| Note       | Reason                                                                                                                                                                                                        |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `1b04affb` | "Codex QA seems broken" has no repro or expected result. Request a concrete case before scheduling.                                                                                                           |
| `bbf98473` | Agent activity vanishing after a turn is important but predates this Agents follow-up and needs a transcript-persistence design. Capture one reproducible example, then plan a separate chat-activity effort. |

The two `deleted: true` source entries are excluded. Do not revive them as tasks without a new report.

## One-at-a-time execution protocol

1. Start with `9cb0a713` (human-chat delivery). For each item, record the build/commit, route, account/project conditions, steps, expected behavior, actual behavior, and whether #668 already changed it. Mark `fixed already`, `reproduced`, `not reproduced`, `needs decision`, or `deferred`; do not equate a code search with a fix.
2. For a reproduced bug, add a failing focused regression test, make the smallest fix, run the affected package checks and frontend accessibility checks, then verify the human path in browser. Keep each issue in its own commit. Where a report may involve authorization, follow the multibay ownership rules and test the backend boundary rather than only hiding UI.
3. For a product decision, write the smallest proposed behavior and tradeoffs, get maintainer agreement, then implement only the approved slice. Do not treat the note's suggested solution as the requirement.
4. After each accepted slice, update this table with evidence and the commit, then decide whether the next item still matters. Rebase the stacked branch on #668 as needed without pushing changes to #668. Before release, run a targeted smoke matrix: human chat, new/existing agent, dual-open thread, slow initialization, funded model loading, Agent Network live/queued outcomes, and artifact workbench.

The first implementation target is the human-chat `To Agent` regression. The next choice depends on its findings; the default order is duplicate composer, delivery/wake/credential failures, then model fallback. No broad UX redesign is authorized by this triage document.
