{
  "name": "seeded-chat-smoke",
  "steps": [
    {
      "name": "wait body",
      "action": {
        "name": "wait_for_selector",
        "selector": "body",
        "state": "visible",
        "timeout_ms": 15000
      }
    },
    {
      "name": "assert chat path",
      "assert": "return window.location.href.includes('.chat');",
      "retries": 2,
      "recovery": "reload"
    },
    {
      "name": "assert seeded chat tab is visible",
      "assert": "return document.body.innerText.includes('scratch.chat');",
      "retries": 2,
      "recovery": "reload"
    },
    {
      "name": "assert empty chat setup is available",
      "assert": "const body = document.body.innerText; return body.includes('New chat setup') && body.includes('Create chat') && body.includes('No chats yet.');",
      "retries": 2,
      "recovery": "reload"
    }
  ]
}
