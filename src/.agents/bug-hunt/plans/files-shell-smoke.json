{
  "name": "files-shell-smoke",
  "steps": [
    {
      "name": "wait project page",
      "action": {
        "name": "wait_for_selector",
        "selector": "[data-cocalc-project-page]",
        "state": "visible",
        "timeout_ms": 15000
      }
    },
    {
      "name": "assert files route",
      "assert": "return /^\\/projects\\/[0-9a-f-]+\\/files\\/?$/.test(window.location.pathname);",
      "retries": 1,
      "recovery": "reload"
    },
    {
      "name": "assert settled CoCalc title is visible",
      "assert": "return typeof document.title === 'string' && document.title.includes('CoCalc') && !document.title.includes('Loading');"
    },
    {
      "name": "assert exactly one project page root is mounted",
      "assert": "return document.querySelectorAll('[data-cocalc-project-page]').length === 1;"
    },
    {
      "name": "assert project page root carries a project id",
      "assert": "const root = document.querySelector('[data-cocalc-project-page]'); const value = root?.getAttribute('data-cocalc-project-page') ?? ''; return /^[0-9a-f-]{36}$/.test(value);"
    },
    {
      "name": "assert project page root matches the route project id",
      "assert": "const match = window.location.pathname.match(/^\\/projects\\/([0-9a-f-]+)\\//); const root = document.querySelector('[data-cocalc-project-page]'); const value = root?.getAttribute('data-cocalc-project-page') ?? ''; return match != null && value === match[1];"
    },
    {
      "name": "assert exactly one file tab strip is mounted",
      "assert": "return document.querySelectorAll('[data-cocalc-file-tab-strip]').length === 1;"
    },
    {
      "name": "assert file tab strip matches the route project id",
      "assert": "const match = window.location.pathname.match(/^\\/projects\\/([0-9a-f-]+)\\//); const strip = document.querySelector('[data-cocalc-file-tab-strip]'); const value = strip?.getAttribute('data-cocalc-file-tab-strip') ?? ''; return match != null && value === match[1];"
    },
    {
      "name": "assert project navigation shell is visible",
      "assert": "const body = document.body.innerText; return body.includes('Files') && body.includes('New');"
    }
  ]
}
