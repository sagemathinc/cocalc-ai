version: 1
name: lean

steps:
  - uses: cocalc/lean
    timeout: 1200

verify:
  - command -v lean
  - lean --version

publish:
  label: Lean
  slug: lean
  description: Lean 4 theorem prover installed with elan.
  family: lean
  version: stable
  channel: stable
  visibility: public
  tags:
    - lean
    - theorem-proving
    - lake
